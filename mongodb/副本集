# 关键概念

- 副本集： 若干 mongod 实例组成的集群，通常这些实例位于不同的宿主机上。
- master： 作为副本集的数据中心，可接受读写请求。
- secondary：master 节点的备份，只接受读请求。
- arbiter：仲裁节点，不存储数据，只拥有选举权。

# 选举机制

选举的目的是产生一个 master 节点。选举会在出现下列情况之一时发生:

- 初始化副本集
- 副本集加入了新节点
- 更新副本集配置
- secondary 节点与当前的 master 节点失联(10s)
